language: android
dist: xenial  # oder focal
jdk:
  - openjdk11

android:
  components:
    - tools
    - platform-tools
    - build-tools-30.0.3
    - android-30
    - extra-android-m2repository
    - extra-google-m2repository

  licenses:
    - 'android-sdk-preview-license-.+'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'

before_install:
  - sdkmanager "platforms;android-30"
  - sdkmanager "build-tools;30.0.3"
  - sdkmanager --licenses

script:
  - ./gradlew clean build
